# Url shortener web application.

** To run this web application locally, you need to have docker and 1.8 GB of free space. **

** In the root directory, run the following command: **
```
docker compose up --build
```
